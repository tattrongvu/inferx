apiVersion: v1
kind: Service
metadata:
  name: secret-db
  namespace: inferx
spec:
  selector:
    app: secret-db
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: secret-db-pvc
  namespace: inferx
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: secret-db
  namespace: inferx
spec:
  serviceName: "secret-db"
  replicas: 1
  selector:
    matchLabels:
      app: secret-db
  template:
    metadata:
      labels:
        app: secret-db
    spec:
      containers:
      - name: postgres
        image: postgres:14.5
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          value: "secret"
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: inferx-secrets
              key: secret-db-password
        - name: POSTGRES_DB
          value: "secretdb"
        - name: PGDATA
          value: "/data/postgres"
        volumeMounts:
        - name: secret-db-data
          mountPath: /data/postgres
        - name: init-sql
          mountPath: /docker-entrypoint-initdb.d
      volumes:
      - name: init-sql
        configMap:
          name: secret-db-init
      - name: secret-db-data
        persistentVolumeClaim:
          claimName: secret-db-pvc